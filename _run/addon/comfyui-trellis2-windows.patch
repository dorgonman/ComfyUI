diff --git a/nodes.py b/nodes.py
index 0edacfa..3e5a0b1 100644
--- a/nodes.py
+++ b/nodes.py
@@ -51,2 +51,7 @@ comfy_path = os.path.dirname(os.path.dirname(os.path.dirname(__file__)))
 BASE_CACHE_DIR = Path(os.path.dirname(os.path.realpath(__file__))) / "triton_caches"
+if os.name == "nt":
+    SHARED_TRITON_CACHE_DIR = BASE_CACHE_DIR / "shared_windows"
+    SHARED_TRITON_CACHE_DIR.mkdir(parents=True, exist_ok=True)
+    os.environ.setdefault("TRITON_CACHE_DIR", str(SHARED_TRITON_CACHE_DIR))
+
 #os.environ["TRITON_ALWAYS_COMPILE"] = "1"
@@ -342,10 +347,10 @@ class Trellis2LoadModel:
             "required": {
                 "modelname": (["microsoft/TRELLIS.2-4B","visualbruno/TRELLIS.2-4B-FP8","TencentARC/Pixal3D-T"],{"default":"microsoft/TRELLIS.2-4B"}),
-                "backend": (["flash_attn","xformers","sdpa","flash_attn_3"],{"default":"flash_attn"}),
+                "backend": (["sdpa","xformers","flash_attn","flash_attn_3"],{"default":"sdpa"}),
                 "device": (["cpu","cuda"],{"default":"cuda"}),
                 "low_vram": ("BOOLEAN",{"default":True}),
                 "keep_models_loaded": ("BOOLEAN", {"default":True}),
-                "conv_backend": (["spconv","torchsparse","flex_gemm"],{"default":"flex_gemm"}),
-                "sparse_backend": (["xformers","flash_attn"],{"default":"flash_attn"}),
+                "conv_backend": (["spconv","flex_gemm","torchsparse"],{"default":"spconv"}),
+                "sparse_backend": (["xformers","flash_attn"],{"default":"xformers"}),
                 "use_reconviagen": ("BOOLEAN",{"default":False}),
                 #"naf_chunk_size":(["None","144","208","272","336","400","464","528","592","656","720","784","848","912","976","1024"],{"default":"None"}),
@@ -361 +366,15 @@ class Trellis2LoadModel:
         import requests
+
+        if backend == "flash_attn":
+            try:
+                import flash_attn  # noqa: F401
+            except ImportError:
+                print("flash_attn is unavailable; using sdpa instead")
+                backend = "sdpa"
+
+        if sparse_backend == "flash_attn":
+            try:
+                import flash_attn  # noqa: F401
+            except ImportError:
+                print("flash_attn is unavailable; using xformers for sparse attention")
+                sparse_backend = "xformers"
@@ -2653,3 +2672,3 @@ class Trellis2PreProcessImage:
                 # self.rembg_model.cpu()
-        output = input
+        output = input.convert('RGBA')
         output_np = np.array(output)
